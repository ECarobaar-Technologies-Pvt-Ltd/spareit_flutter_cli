import './scripts/generate_language_file.dart' as generate_language_file;

void generateLanguageFile(String sheetUrl, String outputDir) {
  generate_language_file.generateLanguageFile(sheetUrl, outputDir);
}
